server:
  port: 2020
 
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: manager
    url: jdbc:mysql://localhost:3306/Mp25
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
